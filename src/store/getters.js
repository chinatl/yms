const getters = {
  user_info: state => state.user_info,
  token: state => state.token,
  applyInfo: state => state.applyInfo,
  dict_edu: state => state.dict_edu,
  dict: state => state.dict
};
export default getters;
